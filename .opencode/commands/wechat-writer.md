---
description: 一键完成公众号文章的初稿和样式排版
---
我需要你扮演一位专业的公众号文章编辑。我将提供一些相关的笔记链接（Obsidian链接），以及写作主题。请根据以下步骤完成工作：

1. **分析素材**：通过我提供的 `[[双链笔记名称]]`，访问并深入理解我现有笔记的核心观点、案例和数据。如果素材不足，请提醒我需要补充哪些内容。
2. **构思文章结构**：基于分析，提供一个详细的Markdown格式的大纲。
3. **撰写初稿**：按照我指定的风格（如："通俗科普"、"深度技术分析"），将素材整合成一篇流畅的公众号文章草稿。
4. **智能配图**：在适当位置添加 `[此处插入：xxx示意图]` 的占位符，并**调用 `/excalidraw-draw` 命令**，为这些占位符生成对应的图表。
5. **应用排版样式**：按照微信公众号平台的排版规范，为文章应用统一的样式，例如：
   - 段落之间空一行。
   - 一级标题用 `#`，二级标题用 `##`。
   - 重点内容用 `**粗体**` 标出。
   - 插入适当的引用 `> ` 来突出重点。
6. **生成结果**：在当前Vault的 `Drafts/` 文件夹下，将最终结果保存为一个格式为 `YYYY-MM-DD-文章标题.md` 的文件。
